{% extends 'base.html.twig' %}

{% block title %}Furniture{% endblock %}

{% block bodyproper %}
    <h1>Furniture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ furniture.id }}</td>
            </tr>
            <tr>
                <th>Inventory_number</th>
                <td>{{ furniture.inventoryNumber }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ furniture.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ furniture.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a class="my" href="{{ path('furniture_index') }}">back to list</a>

    <a class="my" href="{{ path('furniture_edit', {'id': furniture.id}) }}">edit</a>

    {{ include('furniture/_delete_form.html.twig') }}
{% endblock %}
