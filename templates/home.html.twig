<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Office furniture inventory{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block body %}
        <h1>Office furniture inventory</h1>
        <ul>
            <li><a href="{{ path('i_user_index') }}">IUser</a></li>
            <li><a href="{{ path('model_index') }}">Model</a></li>
            <li><a href="{{ path('furniture_index') }}">Furniture</a></li>
        </ul>
        {% endblock %}
    </body>
</html>
